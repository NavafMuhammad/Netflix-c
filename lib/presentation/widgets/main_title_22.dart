import 'package:flutter/material.dart';

class MainTitle22 extends StatelessWidget {
  const MainTitle22({
    required this.title,
    super.key,
  });

  final String title;

  @override
  Widget build(BuildContext context) {
    return Text(
      title,
      style: const TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
    );
  }
}